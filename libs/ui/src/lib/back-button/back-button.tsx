/* eslint-disable-next-line */
export interface BackButtonProps {}

export function BackButton(props: BackButtonProps) {
    return (
        <div>
            <h1>Welcome to BackButton!</h1>
        </div>
    );
}

export default BackButton;
